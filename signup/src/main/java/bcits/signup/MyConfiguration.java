package bcits.signup;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MyConfiguration {

	/*
	 * @Bean ViewResolver abd() { InternalResourceViewResolver resolver = new
	 * InternalResourceViewResolver(); resolver.setSuffix(".jsp");
	 * resolver.setPrefix("frontend/"); return resolver; }
	 */
}
